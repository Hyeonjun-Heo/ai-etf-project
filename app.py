import streamlit as st

# 페이지 기본 설정
st.set_page_config(
    page_title="AI ETF·배당주 투자 도우미",
    layout="wide"
)

# 사이드바 메뉴
st.sidebar.title("📂 메뉴")
page = st.sidebar.radio(
    "이동할 페이지를 선택하세요",
    ("메인 대시보드", "포트폴리오 추천", "백테스트", "모의투자(가상 계좌)", "설정")
)

# 레이아웃: 왼쪽(메인 내용) / 오른쪽(AI 챗봇 자리)
left_col, right_col = st.columns([2.5, 1])

with left_col:
    if page == "메인 대시보드":
        st.header("📊 메인 대시보드")
        st.write("여기에 전체 포트폴리오 요약, 시장 정보, 보유 자산 요약 등을 보여줄 예정입니다.")
    elif page == "포트폴리오 추천":
        st.header("🧩 포트폴리오 추천")
        st.write("사용자의 나이, 투자 성향, 투자 기간 등을 입력받아 ETF·배당주 포트폴리오를 추천해줄 예정입니다.")
    elif page == "백테스트":
        st.header("⏱ 백테스트")
        st.write("과거 데이터를 기준으로 추천 포트폴리오의 수익률, MDD, 변동성을 계산해서 보여줄 예정입니다.")
    elif page == "모의투자(가상 계좌)":
        st.header("💸 모의투자 (가상 계좌)")
        st.write("가상의 잔고, 매수/매도, 평가손익을 기록하는 모의투자 기능이 들어갈 예정입니다.")
    elif page == "설정":
        st.header("⚙️ 설정")
        st.write("손절/익절 비율, 투자기간, 선호 ETF 타입 등을 저장하는 페이지가 될 예정입니다.")

with right_col:
    st.subheader("🤖 AI 투자 코치 (준비중)")
    st.info(
        "이 영역에는 나중에 OpenAI API를 붙여서\n"
        "1) 투자 개념 설명\n"
        "2) 포트폴리오·백테스트 결과 해설\n"
        "3) 질문/답변 챗봇\n"
        "기능을 넣을 예정입니다."
    )
    st.write("지금은 자리만 잡아두는 단계입니다 🙂")

print("안녕하세요")